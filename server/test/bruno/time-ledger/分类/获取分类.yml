info:
  name: 获取分类
  type: http
  seq: 1

http:
  method: GET
  url: "{{localhost}}/api/v1/categories"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
