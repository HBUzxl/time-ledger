info:
  name: 用户登录
  type: http
  seq: 3

http:
  method: POST
  url: "{{localhost}}/api/v1/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "test_user1@email.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
