info:
  name: 用户注册
  type: http
  seq: 3

http:
  method: POST
  url: "{{localhost}}/api/v1/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "test_user1",
        "email": "test_user1@email.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
