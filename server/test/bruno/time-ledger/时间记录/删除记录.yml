info:
  name: 删除记录
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{localhost}}/api/v1/records/{{record_uuid}}"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5