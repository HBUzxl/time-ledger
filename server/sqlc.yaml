version: "2"
sql:
  - schema: "sql/schema"
    queries: "sql/queries"
    engine: "postgresql"
    gen:
      go:
        package: "store" # 生成的 Go 代码所在的包名
        out: "internal/db/store"
        emit_json_tags: true # 生成 JSON 标签，方便序列化
        emit_prepared_queries: false # 生成预编译查询方法
        emit_interface: false # 生成接口定义，用于mock测试